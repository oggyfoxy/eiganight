# IDE specific files & directories
.idea/
.vscode/
*.swp
*.swo
*~
nbproject/

# Operating System generated files
.DS_Store
Thumbs.db
Desktop.ini

# Composer dependencies (if you were using Composer)
/vendor/
composer.lock

# PHPStorm specific
.phpstorm.meta.php
.phpstorm.
# Netbeans specific
/nbproject/private/

# Log files (if you configure PHP error logging to a local file within the project)
# Adjust the path if your log files are stored elsewhere or named differently.
logs/
*.log 
php-error.log 


# XAMPP or local server specific config files (if any are generated in project dir)
# Usually not needed if your XAMPP config is global

# Sensitive configuration files (IMPORTANT if you create a local config not for repo)
# If you were to create a local_config.php that overrides config.php for your specific
# machine (e.g., with different DB credentials not meant for the repository),
# you would add it here.
# Example:
# local_config.php
# config.local.php

# Any build artifacts or uploaded files directory (if applicable, not in current structure)
# /uploads/
# /dist/

# Node modules (if you ever add frontend build tools)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment specific files
.env
.env.*
!.env.example